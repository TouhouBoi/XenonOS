<?php
/*
  ___  _ __   ___ _   _  ___
 / _ \| '_ \ / _ \ | | |/ _ \
| (_) | | | |  __/ |_| |  __/
 \___/|_| |_|\___|\__, |\___|
                  |___/

oneye is released under the GNU Affero General Public License Version 3 (AGPL3)
 -> provided with this release in license.txt
 -> or via web at www.gnu.org/licenses/agpl-3.0.txt

Copyright © 2005 - 2010 eyeos Team (team@eyeos.org)
             since 2010 Lars Knickrehm (mail@lars-sh.de)
*/

include_once(XENONOS_ROOT.'/'.APP_DIR.'/xenonMail/preferences'.XE_CODE_EXTENSION);
include_once(XENONOS_ROOT.'/'.APP_DIR.'/xenonMail/mail'.XE_CODE_EXTENSION);

function xenonMail_on_Message($params=null) {
	xenonWidgets('updateContent',$params);
}

function xenonMail_on_fullScreen() {
	$GLOBALS['xenonMail_WND_1']->setFullScreen();
}

function xenonMail_on_Resize($params=null) {
	$width = intval($params['arg'][0]);
	$height = intval($params['arg'][1]);
	$GLOBALS['xenonMail_WND_1']->width = $width;
	$GLOBALS['xenonMail_WND_1']->height = $height;
	$GLOBALS['xenonMail_line']->setHeight($height - 58);
	$GLOBALS['xenonMail_menuContainer_box']->setHeight($height - 170);
	$GLOBALS['xenonMail_menuContainer']->setHeight($height - 173);
	
//	$GLOBALS['verticalPanel']->setHeight($height - 184);

	$GLOBALS['splitter']->setWidth($width - 150);
	$GLOBALS['splitter']->setHeight($height - 87);
		$GLOBALS['mailTable']->setWidth($GLOBALS['splitter']->width);
//		$GLOBALS['xenonMail_menuContainer']->setHeight($height - 184);
		$GLOBALS['xenonMail_mailHeader']->setWidth($GLOBALS['splitter']->width);
		$GLOBALS['xenonMail_mailBody']->setHeight($GLOBALS['splitter']->height-$GLOBALS['splitter']->position-87);
		$GLOBALS['xenonMail_mailBody']->setWidth($GLOBALS['splitter']->width);
}

function xenonMail_on_resizeCompose($params=null) {
	$width = $params['arg'][0];
	$height = $params['arg'][1];
	$GLOBALS['xenonMail_sendContainer']->setWidth($width);
	$GLOBALS['xenonMail_to_TXT']->setWidth($width-100);
	$GLOBALS['xenonMail_cc_TXT']->setWidth($width-100);
	$GLOBALS['xenonMail_cco_TXT']->setWidth($width-100);
	$GLOBALS['xenonMail_sbj_TXT']->setWidth($width-100);
	$GLOBALS['xenonMail_textareaSend']->setWidth($width);
	$GLOBALS['splitter_compose']->setHeight($height-230);
	$GLOBALS['splitter_compose']->setWidth($width);
	$GLOBALS['splitter_compose']->setPosition($GLOBALS['splitter_compose']->height-80);
	$GLOBALS['xenonMail_Sortabletable']->setWidth($width);
}

function xenonMail_on_preferences($params=null) {
	xenonMail_preferencesWindow();
}

function xenonMail_on_prefGeneral($params=null) {
	xenonMail_generalClick();
}

function xenonMail_on_prefAccounts($params=null) {
	xenonMail_accountsClick();
}

function xenonMail_on_prefSecurity($params=null) {
	xenonMail_securityClick();
}

function xenonMail_on_xenonMail_addButton($params=null) {
	xenonMail_createAccount();
}

function xenonMail_on_cancelAccButton($params=null) {
	if (is_object($GLOBALS['xenonMail_createAccountWND'])) {
		$GLOBALS['xenonMail_createAccountWND']->close();
	}
	if (is_object($GLOBALS['xenonMail_preferencesWND'])) {
		$GLOBALS['xenonMail_preferencesWND']->unhide();
	}
}

function xenonMail_on_backAccButton($params=null) {
	$GLOBALS['xenonMail_createAccountBox2']->hide();
	$GLOBALS['xenonMail_createAccountBox']->unhide();
	if(isset($GLOBALS['xenonMail_createAccountOption1'])) {
		$GLOBALS['xenonMail_createAccountOption1']->remove();
	}
	if(isset($GLOBALS['xenonMail_createAccountOption2'])) {
		$GLOBALS['xenonMail_createAccountOption2']->remove();
	}
}

function xenonMail_on_nextAccButton($params=null) {
	$GLOBALS['xenonMail_createAccountBox2']->unhide();
	$GLOBALS['xenonMail_createAccountBox']->hide();
	xenonMail_createAccount2();
}

function xenonMail_on_new($params=null) {
	$myHidden = new Hidden(array(
		'name'=>'attachments',
		'father'=>'xenonApps',
		'text'=>array()
	));
	xenonWidgets('serialize',array($myHidden));
	$myHidden2 = new Hidden(array(
		'name'=>'focused',
		'father'=>'xenonApps',
		'text'=>'body'
	));
	xenonWidgets('serialize',array($myHidden2));
	xenonMail_compose();
}

function xenonMail_on_finishButton($params=null) {
	xenonMail_finalize1();
}

function xenonMail_on_finishButton2($params=null) {
	xenonMail_finalize2();
}

function xenonMail_on_resizeSplit($params=null) {
	$first = $params['arg'][0];
	$last = $params['arg'][1];

	if(is_numeric($first)) {
		$myHidden = new Hidden(array(
			'name'=>'splitSize',
			'father'=>'xenonApps',
			'text'=>intval($first)
		));
		xenonWidgets('serialize',array($myHidden));
	}

	$first = intval($first);
	$last = intval($last);
	$GLOBALS['mailTable']->setHeight($first);
	$GLOBALS['xenonMail_mailBody']->setHeight($last-95);
	$GLOBALS['splitter']->setValue($first);
}

function xenonMail_on_resizeSplitCompose($params=null) {
	$first = $params['arg'][0];
	$last = $params['arg'][1];
	$first = intval($first);
	$last = intval($last);

	$GLOBALS['xenonMail_textareaSend']->setHeight($first);
	$GLOBALS['xenonMail_Sortabletable']->setHeight($last);
}

function xenonMail_on_sendMail($params=null) {
	$GLOBALS['xenonMail_activityImgContainer']->unhide();
	$GLOBALS['xenonMail_Compose']->close();
	xenonx('sendMessage',array('msg'=>'PerformSendMail'));
}

function xenonMail_on_PerformSendMail($params=null) {
	$to = $GLOBALS['xenonMail_to_TXT']->text;
	$cc = $GLOBALS['xenonMail_cc_TXT']->text;
	$cco = $GLOBALS['xenonMail_cco_TXT']->text;
	$sbj = $GLOBALS['xenonMail_sbj_TXT']->text;
	$body = $GLOBALS['xenonMail_textareaSend']->text;
	xenonMail_sendMail($to,$cc,$cco,$sbj,$body);
	$GLOBALS['xenonMail_activityImgContainer']->hide();
}

function xenonMail_on_attachMail($params=null) {
	global $checknum;
	$options = array(
		XENONDIALOG_TYPE_OPENFILE,
		'SelectFileOpen',
		$checknum,
		'',
		array(
			'All files' => '*.*'
		)
	);
	proc('launch',array('xenonDialog',$options));
}

function xenonMail_on_SelectFileOpen($params=null) {
	$file = $params['arg'][0];
	if($file) {
		$file = um('getCurrentUserDir').'/'.FILES_USER_DIR.$file;
		if(!count($GLOBALS['attachments']->text)) {
			$GLOBALS['splitter_compose']->showSplitter();
			$GLOBALS['splitter_compose']->setPosition($GLOBALS['splitter_compose']->height-80);
			$GLOBALS['xenonMail_Sortabletable']->unhide();
		}
		$GLOBALS['attachments']->text[] = $file;
		$GLOBALS['xenonMail_Sortabletable']->addRow(array(basename($file),vfs('fileSize',array($file))));
	}
}

function xenonMail_on_changeFocus($params=null) {
	$myHidden = new Hidden(array(
		'name'=>'focused',
		'father'=>'xenonApps',
		'text'=>$params['field'][0]
	));
	xenonWidgets('serialize',array($myHidden));
}

function xenonMail_on_contactsMail($params=null) {
	/**
	$field = $GLOBALS['focused']->text;
	if($field == 'body') {
		$field = 'xenonMail_textareaSend';
	} else {
		$field = 'xenonMail_'.$field.'_TXT';
	}

	$GLOBALS[$field]->setText('lol');
	**/
	xenonMail_drawContacts();
}

function xenonMail_on_Close($params=null) {
	if(is_object($GLOBALS['splitSize'])) {
		$spSize = $GLOBALS['splitSize']->text;
		$config = xenonXML('getXMLconfig',array('xenonMail','settings.xml'));
		$config['xenonMail'][0]['splitPos'][0] = $spSize;
		xenonXML('setXMLconfig',array('xenonMail','settings.xml',$config));
	}
	proc('end');
}

function xenonMail_on_closePreferences($params=null) {
	$GLOBALS['xenonMail_menuContainer']->remove();
	xenonMail_drawBoxes();
}

function xenonMail_on_openMail($params=null) {
	$userconfig = $GLOBALS['defaultMail']->text;
	$uid = $GLOBALS['mailTable']->selected;
	$user = $userconfig['typeuser'][0];
	$password = $userconfig['typepassword'][0];
	$server = $userconfig['typehost'][0];
	$port = $userconfig['typeport'][0];
	$type = $userconfig['type'][0];
	if ($userconfig['typessl'][0] == 0) {
		$usessl = '';
	} else {
		$usessl = '/ssl';
	}
	$userconfig = $GLOBALS['defaultMail']->text;

	$dbPath = um('getCurrentUserDir').'/conf/xenonMail/db_'.$userconfig['id'][0].'.sqlite';
	$sqliteError = null;
	$dbHandle = sqlite_open($dbPath, 0666, $sqliteError) or die($sqliteError);
	if($GLOBALS['location']->text == 'inbox') {
		$sqlGetView = 'SELECT * FROM mails WHERE uid = '.intval($uid);
	} else {
		$sqlGetView = 'SELECT * FROM sent WHERE id = '.intval($uid);
	}

	//$sqlGetView = 'SELECT * FROM mails';
	$query = sqlite_query($dbHandle, $sqlGetView);
	$result = sqlite_fetch_all($query, SQLITE_ASSOC);
	$GLOBALS['xenonMail_lfrom']->setText('From: %s',array($result[0]['sender']));
	$GLOBALS['xenonMail_lsubject']->setText('Subject: %s',array($result[0]['subject']));
	$GLOBALS['xenonMail_ldate']->setText('Date: %s',array($result[0]['date']));
	$GLOBALS['xenonMail_lto']->setText('To: %s',array($result[0]['to']));

	if($type == 'pop') {
		$body = $result[0]['body'];
	} elseif($GLOBALS['location']->text == 'inbox') {
		$connection = imap_open('{'.$server.':'.$port.$usessl.'/novalidate-cert}INBOX',$user,$password);
		$body = get_part($connection,$uid,'TEXT/PLAIN');
	} else {
		$body = $result[0]['body'];
	}
	$GLOBALS['xenonMail_mailBody']->setText($body);
	$attachments = list_attachments($connection,$uid);
}

function xenonMail_on_openMailBig($params) {
	$userconfig = $GLOBALS['defaultMail']->text;
	$uid = $GLOBALS['mailTable']->selected;
	$user = $userconfig['typeuser'][0];
	$password = $userconfig['typepassword'][0];
	$server = $userconfig['typehost'][0];
	$port = $userconfig['typeport'][0];
	$type = $userconfig['type'][0];
	if ($userconfig['typessl'][0] == 0) {
		$usessl = '';
	} else {
		$usessl = '/ssl';
	}
	$userconfig = $GLOBALS['defaultMail']->text;

	$dbPath = um('getCurrentUserDir').'/conf/xenonMail/db_'.$userconfig['id'][0].'.sqlite';
	$sqliteError = null;
	$dbHandle = sqlite_open($dbPath, 0666, $sqliteError) or die($sqliteError);
	if($GLOBALS['location']->text == 'inbox') {
		$sqlGetView = 'SELECT * FROM mails WHERE uid = '.intval($uid);
	} else {
		$sqlGetView = 'SELECT * FROM sent WHERE id = '.intval($uid);
	}

	//$sqlGetView = 'SELECT * FROM mails';
	$query = sqlite_query($dbHandle, $sqlGetView);
	$result = sqlite_fetch_all($query, SQLITE_ASSOC);

	if($type == 'pop') {
		$body = $result[0]['body'];
	} elseif($GLOBALS['location']->text == 'inbox') {
		$connection = imap_open('{'.$server.':'.$port.$usessl.'/novalidate-cert}INBOX',$user,$password);
		$body = get_part($connection,$uid,'TEXT/PLAIN');
	} else {
		$body = $result[0]['body'];
	}
	$attachments = list_attachments($connection,$uid);
	xenonMail_compose($result[0]['sender'],$result[0]['subject'],$body,1,$attachments);
}

function xenonMail_on_openMailBox($params) {
	$id = $params['mailbox'][0];
	$config = xenonXML('getXMLConfig',array('xenonMail','accounts.xml'));
	foreach($config['xenonMail'][0]['accounts'][0]['account'] as $value) {
		if(intval($value['id'][0]) == $id) {
			$myHidden = new Hidden(array(
				'name'=>'defaultMail',
				'father'=>'xenonApps',
				'text'=>$value
			));
			xenonWidgets('serialize',array($myHidden));
		}
	}
	$myHidden4 = new Hidden(array('name'=>'location','father'=>'xenonApps','text'=>'inbox'));
	xenonWidgets('serialize',array($myHidden4));
	xenonMail_on_printMails();
}

function xenonMail_on_openMailSent($params) {
	$id = $params['mailbox'][0];
	$config = xenonXML('getXMLConfig',array('xenonMail','accounts.xml'));
	foreach($config['xenonMail'][0]['accounts'][0]['account'] as $value) {
		if(intval($value['id'][0]) == $id) {
			$myHidden = new Hidden(array(
				'name'=>'defaultMail',
				'father'=>'xenonApps',
				'text'=>$value
			));
			xenonWidgets('serialize',array($myHidden));
		}
	}
	$myHidden4 = new Hidden(array('name'=>'location','father'=>'xenonApps','text'=>'sent'));
	xenonWidgets('serialize',array($myHidden4));
	xenonMail_on_printSent();
}

function xenonMail_on_delete($params) {
	$content = 'Do you really want to delete this mail?';
	$title = 'Are you sure?';
	xenonx('messageBox',array(
		'btn1_capt' => 'Yes',
		'btn1_name' => 'doDelete',
		'btn2_capt' => 'No',
		'btn2_name' => 'noDelete',
		'content' => $content,
		'img' => 'index.php?theme=' . $_SESSION['usertheme'] . '&extern=icons/48x48/editClean.png',
		'savePosition' => 1,
		'title' => $title,
		'type' => 3,
		'win_name' => 'deleteMail_Window',
		'win_style' => TITLE
	));
}

function xenonMail_on_noDelete($params) {
	$GLOBALS['deleteMail_Window']->close();
}

function xenonMail_on_doDelete($params) {
	$userconfig = $GLOBALS['defaultMail']->text;
	$uid = $GLOBALS['mailTable']->selected;
	$user = $userconfig['typeuser'][0];
	$password = $userconfig['typepassword'][0];
	$server = $userconfig['typehost'][0];
	$port = $userconfig['typeport'][0];
	$type = $userconfig['type'][0];
	if ($userconfig['typessl'][0] == 0) {
		$usessl = '';
	} else {
		$usessl = '/ssl';
	}
	$userconfig = $GLOBALS['defaultMail']->text;
	$dbPath = um('getCurrentUserDir').'/conf/xenonMail/db_'.$userconfig['id'][0].'.sqlite';
	$sqliteError = null;
	$dbHandle = sqlite_open($dbPath, 0666, $sqliteError) or die($sqliteError);
	if($GLOBALS['location']->text == 'inbox') {
		$sqlGetView = 'DELETE FROM mails WHERE uid = '.intval($uid);
		//$sqlGetView = 'SELECT * FROM mails';
		$query = sqlite_query($dbHandle, $sqlGetView);
		$result = sqlite_fetch_all($query, SQLITE_ASSOC);

		$connection = imap_open('{'.$server.':'.$port.$usessl.'/novalidate-cert}INBOX',$user,$password);
		imap_delete($connection, $uid);
		imap_expunge($connection);
	} else {
		$sqlGetView = 'DELETE FROM sent WHERE id = '.intval($uid);
		//$sqlGetView = 'SELECT * FROM mails';
		$query = sqlite_query($dbHandle, $sqlGetView);
		$result = sqlite_fetch_all($query, SQLITE_ASSOC);
	}
	$GLOBALS['mailTable']->delRow($uid);
	$GLOBALS['deleteMail_Window']->close();
}

function xenonMail_on_openAttachment($params) {
	global $checknum;
	$options = array(
		XENONDIALOG_TYPE_SAVEFILE,
		'SelectFileSave',
		$checknum,
		'',
		array(
			'All files' => '*.*'
		)
	);
	proc('launch',array('xenonDialog',$options));
}

function xenonMail_on_SelectFileSave($params) {
	$file = $params['arg'][0];
	$arr = /* utf8 */ explode(',', $GLOBALS['xenonMail_Sortabletable']->selected);
	$id = $arr[0];
	$type = $arr[1];
	$userconfig = $GLOBALS['defaultMail']->text;
	$user = $userconfig['typeuser'][0];
	$password = $userconfig['typepassword'][0];
	$server = $userconfig['typehost'][0];
	$port = $userconfig['typeport'][0];
	$type = $userconfig['type'][0];
	if ($userconfig['typessl'][0] == 0) {
		$usessl = '';
	} else {
		$usessl = '/ssl';
	}
	$connection = imap_open('{'.$server.':'.$port.$usessl.'/novalidate-cert}INBOX',$user,$password);

	$body = getdecodevalue(imap_fetchbody($connection,$GLOBALS['mailTable']->selected,$id),$type);
	$file = um('getCurrentUserDir').FILES_USER_DIR.'/'.$file;
	if(!vfs('create',array($file,'xenonMail'))) {
		xenonx('messageBox',array('content'=>'Unable to create file'));
		return;
	}
	$fp = vfs('open',array($file,'w'));
	if(!$fp) {
		xenonx('messageBox',array('content'=>'Sorry, you do not have sufficient permissions.'));
		return;
	}
	fwrite($fp,$body);
	fclose($fp);
}

function getdecodevalue($message,$coding) {
	if($coding == 'imap') {
		$message = imap_base64($message);
	}elseif ($coding == 0) {
		$message = imap_8bit($message);
	} elseif ($coding == 1) {
		$message = imap_8bit($message);
	} elseif ($coding == 2) {
		$message = imap_binary($message);
	} elseif ($coding == 3) {
		$message=imap_base64($message);
	} elseif ($coding == 4) {
		$message = imap_qprint($message);
	} elseif ($coding == 5) {
		$message = imap_base64($message);
	}
	return $message;
}

function xenonMail_on_reply($params) {
	$userconfig = $GLOBALS['defaultMail']->text;
	$uid = $GLOBALS['mailTable']->selected;
	$user = $userconfig['typeuser'][0];
	$password = $userconfig['typepassword'][0];
	$server = $userconfig['typehost'][0];
	$port = $userconfig['typeport'][0];
	$type = $userconfig['type'][0];
	if ($userconfig['typessl'][0] == 0) {
		$usessl = '';
	} else {
		$usessl = '/ssl';
	}
	$userconfig = $GLOBALS['defaultMail']->text;

	$dbPath = um('getCurrentUserDir').'/conf/xenonMail/db_'.$userconfig['id'][0].'.sqlite';
	$sqliteError = null;
	$dbHandle = sqlite_open($dbPath, 0666, $sqliteError) or die($sqliteError);

	$sqlGetView = 'SELECT * FROM mails WHERE uid = '.intval($uid);

	//$sqlGetView = 'SELECT * FROM mails';
	$query = sqlite_query($dbHandle, $sqlGetView);
	$result = sqlite_fetch_all($query, SQLITE_ASSOC);

	if ( /* utf8 */ substr($result[0]['subject'], 0, 3) !== 'Re:') {
		$result[0]['subject'] = 'Re: ' . $result[0]['subject'];
	}
	$body .= $result[0]['sender']." wrote:\n";
	$body .= ">";
	$body .= str_replace("\n","\n>",$GLOBALS['xenonMail_mailBody']->text);
	$body .= "\n\n";
	xenonMail_compose($result[0]['sender'],$result[0]['subject'],$body);
}

function xenonMail_on_forward($params) {
	$userconfig = $GLOBALS['defaultMail']->text;
	$uid = $GLOBALS['mailTable']->selected;
	$user = $userconfig['typeuser'][0];
	$password = $userconfig['typepassword'][0];
	$server = $userconfig['typehost'][0];
	$port = $userconfig['typeport'][0];
	$type = $userconfig['type'][0];
	if ($userconfig['typessl'][0] == 0) {
		$usessl = '';
	} else {
		$usessl = '/ssl';
	}
	$userconfig = $GLOBALS['defaultMail']->text;

	$dbPath = um('getCurrentUserDir').'/conf/xenonMail/db_'.$userconfig['id'][0].'.sqlite';
	$sqliteError = null;
	$dbHandle = sqlite_open($dbPath, 0666, $sqliteError) or die($sqliteError);

	$sqlGetView = 'SELECT * FROM mails WHERE uid = '.intval($uid);

	//$sqlGetView = 'SELECT * FROM mails';
	$query = sqlite_query($dbHandle, $sqlGetView);
	$result = sqlite_fetch_all($query, SQLITE_ASSOC);

	if ( /* utf8 */ substr($result[0]['subject'], 0, 4) !== 'Fwd:') {
		$result[0]['subject'] = 'Fwd: ' . $result[0]['subject'];
	}
	$body .= "\n\n\n\n------ Forwarded Message ---------\n";
	$body .= "From: ".$result[0]['sender']."\n";
	$body .= 'To: '.$result[0]['to']."\n";
	$body .= 'Subject: '.$result[0]['subject']."\n\n";
	$body .= $GLOBALS['xenonMail_mailBody']->text;
	$body .= "\n\n";
	xenonMail_compose($result[0]['sender'],$result[0]['subject'],$body);
}

function get_mime_type(&$structure) {
	$primary_mime_type = array("TEXT", "MULTIPART","MESSAGE", "APPLICATION", "AUDIO","IMAGE", "VIDEO", "OTHER");
	if($structure->subtype) {
		return $primary_mime_type[(int) $structure->type] . '/' .$structure->subtype;
	}
	return "TEXT/PLAIN";
}

function get_part($stream, $msg_number, $mime_type, $structure = false,$part_number    = false) {
	if(!$structure) {
		$structure = imap_fetchstructure($stream, $msg_number);
	}
	if($structure) {
		if($mime_type == get_mime_type($structure)) {
			if(!$part_number) {
				$part_number = "1";
			}
			$text = imap_fetchbody($stream, $msg_number, $part_number);
			if($structure->encoding == 3) {
				return imap_base64($text);
			} else if($structure->encoding == 4) {
				return decode_qprint($text);
			} else {
			return $text;
		}
	}

		if($structure->type == 1) /* multipart */ {
		while(list($index, $sub_structure) = each($structure->parts)) {
			if($part_number) {
				$prefix = $part_number . '.';
			}
			$data = get_part($stream, $msg_number, $mime_type, $sub_structure,$prefix .    ($index + 1));
			if($data) {
				return $data;
			}
		}
		}
	}
	return false;
}

function list_attachments($connection,$msg) {
	$structure = imap_fetchstructure($connection, $msg);
	$parts = $structure->parts;
	$i = 1;
	foreach($parts as $part) {
		if($part->disposition == 'ATTACHMENT') {
			$filename = $part->dparameters[0]->value;
			$return[] = array($filename,$part->bytes,$i,$part->type);
		}
		$i++;
 	}
 
 	return $return;
}

function xenonMail_ByteSize($bytes) {
	$size = $bytes / 1024;
    if($size < 1024) {
        $size = /* utf8 */ number_format($size, 2);
        $size .= ' KB';
    } else  {
        if($size / 1024 < 1024) {
            $size = /* utf8 */ number_format($size / 1024, 2);
            $size .= ' MB';
        } else if ($size / 1024 / 1024 < 1024) {
            $size = /* utf8 */ number_format($size / 1024 / 1024, 2);
            $size .= ' GB';
        }
    }
    return $size;
}

function decode_qprint($str) {
    $str = /* utf8 */ preg_replace('/\\=([A-F][A-F0-9])/', '%$1', $str);
    $str = urldecode($str);
    $str = utf8_encode($str);
    return $str;
}
?>